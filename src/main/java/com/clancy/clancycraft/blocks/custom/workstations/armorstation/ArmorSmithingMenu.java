package com.clancy.clancycraft.blocks.custom.workstations.armorstation;

import net.minecraft.world.Container;
import net.minecraft.world.SimpleContainer;
import net.minecraft.world.entity.player.Inventory;
import net.minecraft.world.entity.player.Player;
import net.minecraft.world.inventory.AbstractContainerMenu;
import net.minecraft.world.inventory.Slot;
import net.minecraft.world.item.ItemStack;

//public class ArmorSmithingMenu extends AbstractContainerMenu {
   // private final Container container;

   // public ArmorSmithingMenu(int id, Inventory playerInv) {
   //     this(id, playerInv, new SimpleContainer(5));
  //  }

   // public ArmorSmithingMenu(int id, Inventory playerInv, Container container) {
     //   super(ModMenus.ARMOR_SMITHING.get(), id);
    //    this.container = container;

        // INPUT SLOTS (armor parts)
     //   this.addSlot(new Slot(container, 0, 30, 34));
     //   this.addSlot(new Slot(container, 1, 50, 34));
     //   this.addSlot(new Slot(container, 2, 70, 34));

        // OUTPUT SLOT
  //      this.addSlot(new Slot(container, 3, 124, 34) {
   //         @Override
    //        public boolean mayPlace(ItemStack stack) {
   //             return false;
   //         }
   //     });

        // PLAYER INVENTORY
   //     for (int row = 0; row < 3; row++) {
     //       for (int col = 0; col < 9; col++) {
     //           this.addSlot(new Slot(playerInv, col + row * 9 + 9,
    //                    8 + col * 18, 84 + row * 18));
  //          }
   //     }

        // HOTBAR
  //      for (int col = 0; col < 9; col++) {
   //         this.addSlot(new Slot(playerInv, col, 8 + col * 18, 142));
  //      }
   // }

  //  @Override
  //  public boolean stillValid(Player player) {
  //      return true;
 //   }
//}

